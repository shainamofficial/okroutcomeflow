// Shared types, zod schemas, and the permissions module live here.
// Both apps/web and apps/api import from "@okroutcomeflow/shared".

export * from "./permissions";

export const SHARED_PACKAGE_VERSION = "0.1.0";
